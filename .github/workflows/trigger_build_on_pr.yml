name: Trigger Build on Pull Request

on:
  pull_request:  
    types: [ opened, synchronize, reopened ]
    
jobs:

  build:
    runs-on: ubuntu-latest
    name: Execute Workflow  

    steps:

    - name: Extract Branch
      id: extract_branch
      run: |
        echo GITHUB_REF: ${GITHUB_REF#refs/heads/}
        echo PR: ${{ github.event.pull_request.head.ref }}
        echo "::set-output name=branch::$(echo ${GITHUB_REF#refs/heads/})"

    - name: Build Docker Image
      uses: aurelien-baudet/workflow-dispatch@v2
      with:
        workflow: Build and Publish Docker Image
        token: ${{ secrets.ACTIONS_ACCESS_TOKEN }}
        ref: ${{ github.event.pull_request.head.ref }}
        inputs: '{ "git_commit": "${{ github.event.pull_request.head.ref }}" }'

    - name: Build VM
      uses: aurelien-baudet/workflow-dispatch@v2
      with:
        repo: jamesfera/actionstest
        ref: ${{ github.event.pull_request.head.ref }}
        workflow: Create and Bootstrap Virtual Machine
        token: ${{ secrets.ACTIONS_ACCESS_TOKEN }}
        inputs: '{ "name": "${{ github.event.pull_request.head.ref }}", "swarm": "true" }'

    - name: Deploy
      uses: aurelien-baudet/workflow-dispatch@v2
      with:
        wait-for-completion: false
        ref: ${{ github.event.pull_request.head.ref }}
        repo: jamesfera/actionstest
        workflow: Docker Image Deployment
        token: ${{ secrets.ACTIONS_ACCESS_TOKEN }}
        inputs: '{ "host": "aexautodeploy${{ github.event.pull_request.head.ref }}.eastus.cloudapp.azure.com", "deployment_subdomain": "${{github.event.pull_request.head.ref}}", "apiimagetag": "develop", "uiimagetag": "${{github.event.pull_request.head.ref}}" }'